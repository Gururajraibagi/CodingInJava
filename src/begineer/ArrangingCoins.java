package begineer;

public class ArrangingCoins {

    // r = -1+sqrt(1+2n) / 2

    // (n/2) x (n+1)
    /*
     * public static void main(String[] args) { int n = 1804289383; Math.floor(n) int ans = (int)
     * (-1 + Math.sqrt(1 + (8 * n))) / 2; System.out.println(ans); Integer.MIN_VALUE }
     */
}
